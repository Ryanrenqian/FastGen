# HYP-20260810-09: TFD ImageNet-64 official DMD2 LMDB retraining

## Hypothesis

The prior HF Parquet ImageNet-64 preprocessing is distributionally incompatible with the NVIDIA EDM reference statistics. Training the same pinned TFD recipe on the official DMD2 LMDB should substantially reduce the paper-reference FID from `12.019839387829222` toward the reported `1.58`.

## Fixed variables

- Architecture, feature layers, optimizer, scheduler, precision, seed, seven-GPU batch semantics, and 200,000 optimizer updates remain identical to HYP-20260807-08.
- Evaluation remains one-step, conditioning sigma 80, 50,000 balanced labels, official NVIDIA Inception `.pkl`, and official EDM `imagenet-64x64.npz`.
- Teacher checkpoint remains `/mnt/home/renqian/imgGen/runtime/MODEL/imagenet-64/edm-imagenet-64x64-cond-adm.pth`.

## Changed variable

- Training data changes from `benjamin-paine/imagenet-1k-64x64` Parquet-derived zip to the official TFD/DMD2 LMDB:
  `https://huggingface.co/tianweiy/DMD2/resolve/main/data/imagenet/imagenet-64x64_lmdb.zip`.
- Pinned DMD2 revision: `be22767697a1f3ca656b73c776e15fa335c86c6c`.
- Mirror download size/expected SHA-256: `13,978,021,501` bytes / `36887fdc4a40de0deedc261db00a92530dbc3a991b5ff50ac9999397e4890a41`.
- Server dataset target: `/mnt/home/renqian/imgGen/runtime/DATA/imagenet-64/imagenet-64x64_lmdb`.

## Isolation and gates

- Branch/worktree: `feature/tfd-imagenet64-paper` / `/private/tmp/FastGen-tfd-imagenet64-paper`.
- Old HF dataset, checkpoints, generated samples, and FID files are retained.
- New run name: `tfd_in64_paper_lmdb_200k_s10`.
- Preflight gates: archive hash and structure verified; LMDB shapes `[1281167,3,64,64]` and `[1281167]`; all 1,000 classes nonempty; loader unit test; TFD tests 6/6; 10-update seven-GPU smoke with finite gradients and no OOM.
- Full training and official-reference FID require the smoke gates to pass.

## Status

- Dataset archive downloaded through `hf-mirror.com`; size `13,978,021,501` bytes and SHA-256 `36887fdc4a40de0deedc261db00a92530dbc3a991b5ff50ac9999397e4890a41` both match the pinned Hugging Face metadata.
- Extracted LMDB size: 20 GiB. Verified shapes: images `(1,281,167,3,64,64)`, labels `(1,281,167,)`; all 1,000 classes are nonempty with 732--1,300 images/class.
- Real loader batch: condition `(10,1000)`, positives and anchors each `(10,4,3,64,64)`, normalized range `[-1,1]`.
- Server unit tests: 7/7 passed with `lmdb==2.3.0`.
- Implementation commit: `873eed2`.
- Smoke command:

```bash
FASTGEN_OUTPUT_ROOT=/mnt/home/renqian/imgGen/runtime/fastgen \
DATA_ROOT_DIR=/mnt/home/renqian/imgGen/runtime/DATA \
CKPT_ROOT_DIR=/mnt/home/renqian/imgGen/runtime/MODEL \
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6 \
/root/miniconda3/envs/fastgen/bin/torchrun --standalone --nproc_per_node=7 train.py \
  --config=fastgen/configs/experiments/EDM/config_tfd_in64.py - \
  trainer.ddp=True trainer.seed=10 trainer.max_iter=11 \
  trainer.logging_iter=1 trainer.save_ckpt_iter=10 trainer.resume=False \
  log_config.name=tfd_in64_paper_lmdb_smoke_s10 log_config.wandb_mode=online
```

- Smoke result: pending.
- Result grade: exploratory until full training and official-reference FID complete.
